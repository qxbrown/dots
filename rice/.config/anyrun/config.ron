Config(
  x: Fraction(0.5),
  y: Fraction(0.1),
  width: Absolute(800),
  height: Absolute(0),

  hide_icons: false,
  ignore_exclusive_zones: false,
  layer: Overlay,

  hide_plugin_info: true,
  close_on_click: false,
  show_results_immediately: false,
  max_entries: None,

  plugins: [
    "libapplications.so",
    "libshell.so",
    "librink.so",
    "libwebsearch.so",
    "libdictionary.so",
  ],
)
